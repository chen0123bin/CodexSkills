project_name: "{{PROJECT_NAME}}"
current_version: "{{VERSION}}"
current_phase: "{{PHASE}}"
updated_at: "{{TIMESTAMP}}"

versions:
  {{VERSION}}:
    status: "active"
    phase: "{{PHASE}}"
    type: "{{TYPE}}"
    spec: ""
    plan: ""
