# 项目记忆：{{PROJECT_NAME}}

> 最后更新：{{TIMESTAMP}}
> 时间格式：yyyy-MM-dd HH:mm:ss

## 写入规则

- 每次使用本 skill 前先读取本文件。
- 只有用户明确要求“记住”“以后默认这样”“作为项目约定”时才写入。
- 只记录跨版本、跨阶段仍然有效的事实、约束、偏好和决策。
- 不记录执行流水、命令输出、临时判断或普通阶段总结。

## 记忆条目

| 时间 | 原因 | 内容 |
|------|------|------|
