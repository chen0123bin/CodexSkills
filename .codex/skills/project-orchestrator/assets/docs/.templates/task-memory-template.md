# 任务记忆：T{XXX} - {任务标题}

> 版本: v{X} | 最后更新: {date} | 用途: 记录当前 task 已发生的局部事实 | 时间格式: 本地时间 `【YYYY-MM-DD HH:MM:SS】`

---

## 使用规则

- 只记录已经发生的事实，不写未经确认的未来计划。
- 只保留后续执行、审查和交付真正需要的上下文。
- 每次修改代码后，优先更新“变更文件记录”和“验证记录”。

---

## 变更文件记录

| 时间（本地） | 文件 | 动作 | 变更摘要 |
|------|------|------|----------|
| {datetime} | `{file_path}` | 新增 / 修改 / 删除 | {做了什么，为什么改} |

---

## 关键实现决策

| 时间（本地） | 决策 | 原因 | 影响范围 |
|------|------|------|----------|
| {datetime} | {决策内容} | {为什么这样做} | {影响了哪些模块/任务} |

---

## 验证记录

| 时间（本地） | 验证方式 | 结果 | 备注 |
|------|----------|------|------|
| {datetime} | `{command}` / 手动检查 | 通过 / 失败 | {补充说明} |

---

## 阻塞与交接

- **当前阻塞**: 无 / {阻塞原因}
- **待用户确认**: 无 / {需要用户决定的点}
- **下一个任务需要知道的事**: {只保留必要摘要}
