interface:
  display_name: "Markdown 转 DOCX"
  short_description: "用 pandoc 将 Markdown 转为 DOCX，默认套用内置 Word 模板"
  default_prompt: "使用 $markdown-to-docx 将一个 Markdown 文件或文章文件夹转换为带图片、题注和默认 Word 模板的 .docx 文件；如我提供模板路径，则使用该模板。"

policy:
  allow_implicit_invocation: true
